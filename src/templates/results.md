# AWS Lambda shell commands for `$RUNTIME` runtime
### Last change detected: `$LAST_UPDATED` from `$AWS_REGION`
$VERSIONS
## Available AWS Lambda shell commands, shortcuts, and syntax

$SHELL_COMMANDS
